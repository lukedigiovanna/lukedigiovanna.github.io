import styled from "styled-components"

const Timeline = styled.div`
    overflow-x: scroll;
    max-width: 800px;
`

export const Projects = () => {
    return (
        <Timeline>
            
        </Timeline>
    )
}